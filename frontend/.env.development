# Loaded automatically by `npm run dev`.
#
# A relative base means requests are same-origin and go through the Vite proxy
# configured in vite.config.ts, which forwards /api to the backend. No CORS, and
# no backend port compiled into the app.
VITE_API_BASE_URL=/api/v1
